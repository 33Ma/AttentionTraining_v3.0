# 游戏化注意力训练系统 · 使用说明书

> 面向中小学校及个人的 AI 驱动注意力训练平台 —— 硬件仅需一台带摄像头的普通电脑。
>
> 文档版本：v3.0 ｜ 更新日期：2026-08-31

---

## 目录

1. [系统简介](#1-系统简介)
2. [功能一览](#2-功能一览)
3. [环境要求](#3-环境要求)
4. [快速开始](#4-快速开始)
5. [角色与权限](#5-角色与权限)
6. [使用指南](#6-使用指南)
7. [设置说明](#7-设置说明)
8. [数据存储与备份](#8-数据存储与备份)
9. [项目结构](#9-项目结构)
10. [开发与构建](#10-开发与构建)
11. [常见问题](#11-常见问题)
12. [注意事项与隐私](#12-注意事项与隐私)

---

## 1. 系统简介

本系统通过电脑摄像头**实时监测用户的眨眼频率、视线专注度和头部姿态**，结合"找茬"和"动态追踪"两类游戏化训练模式，为学生提供客观的注意力评估与个性化训练方案。系统内置学生端 **AI 教练** 与教师端 **AI 助教**，可根据训练数据自动生成分析报告和针对性建议。

### 系统特色

- ✅ **低成本部署**：普通 PC + USB 摄像头即可运行，无需脑电、眼动等专业设备
- ✅ **隐私优先**：摄像头仅提取面部关键点坐标，不录制、不存储、不上传视频画面
- ✅ **离线可用**：核心分析功能（本地 ONNX 模型）完全离线；云端 LLM 分析为可选增强
- ✅ **多角色支持**：管理员 / 教师 / 学生三级权限，适配学校场景
- ✅ **游戏化激励**：20 项成就 + 连击得分 + AI 鼓励，提升训练依从性

---

## 2. 功能一览

| 功能模块 | 说明 |
|---------|------|
| 🎮 游戏化训练 | 找茬模式、动态追踪模式，支持自适应难度与连击得分 |
| 👁️ 实时生物反馈 | 眨眼检测（EAR）、视线专注度、头部姿态（可选）、注意力评分（0-100） |
| 🤖 本地 AI 分析 | 训练结束后即时生成结构化报告（专注等级、疲劳度、综合评分），无需联网 |
| ☁️ 云端 LLM 分析 | 可选对接 OpenAI 兼容 API，生成深度个性化报告 |
| 💬 AI 教练对话 | 学生端专属，基于训练数据多轮对话指导 |
| 📊 教师端 AI 助教 | 班级汇总、单学生下钻、风险提示与训练建议 |
| 🧠 智能推荐 | 根据训练历史自动推荐模式与难度组合 |
| 🏆 成就系统 | 20 项成就（两种模式各 10 项） |
| 📈 数据可视化 | 注意力/得分/眨眼/注视趋势图 + 专注度热力图 |
| 👥 多用户管理 | 管理员可增删改查用户，教师可查看所管学生的数据 |
| 🔄 便携数据同步 | 学生导出 JSON 数据文件，教师导入并合并（不覆盖他人数据） |

---

## 3. 环境要求

### 3.1 硬件要求

| 配置项 | 最低要求 | 推荐配置 |
|--------|---------|---------|
| CPU | 双核 2.0GHz | 四核 2.5GHz+ |
| 内存 | 4GB RAM | 8GB RAM |
| 摄像头 | 支持 OpenCV 的 USB 摄像头 | 720p 及以上 |
| 存储空间 | 500MB | 1GB |
| GPU（可选） | — | NVIDIA GPU + CUDA 11.8+ |

### 3.2 软件要求

| 操作系统 | 版本 |
|---------|------|
| Windows | 10 / 11（64 位，推荐） |
| macOS | 10.15+（Intel / Apple Silicon） |
| Linux | Ubuntu 20.04+ 或主流发行版 |

**源码运行需要 Python**：3.9 ~ 3.11

### 3.3 依赖安装（仅源码运行需要）

```bash
cd attention_training_py
pip install -r requirements.txt
```

> 说明：`onnxruntime-gpu` 默认按纯 CPU 推理使用（模型为毫秒级）。如需 GPU 加速，请安装与系统 CUDA 匹配的 cuDNN/cuBLAS，并设置环境变量 `ATTENTION_TRAINING_USE_GPU=1`。

---

## 4. 快速开始

### 4.1 方式一：直接运行打包版（推荐给学校/非技术人员）

1. 将 `APP.zip` 解压，或直接进入已解压的目录 `App\dist\AttentionTrainingApp\`；
2. 双击 `AttentionTrainingApp.exe` 启动；
3. 使用默认账户登录（见 4.3）。

> 打包版为绿色便携软件：程序运行产生的用户数据（数据库、设置、用户目录等）保存在 **exe 同目录**下，便于拷贝迁移和备份。

### 4.2 方式二：源码运行（开发/调试）

```bash
cd attention_training_py
python run.py
```

也可以直接运行 `python main.py`。`run.py` 会自动把项目根目录加入 Python 路径，推荐使用。

### 4.3 默认账户

系统首次启动时自动创建以下演示账户：

| 角色 | 用户名 | 密码 | 权限说明 |
|------|--------|------|---------|
| 👑 管理员 | `admin` | `admin123` | 全部权限，可管理用户和系统 |
| 👨‍🏫 教师 | `teacher` | `teacher123` | 查看班级报告、管理学生、AI 助教 |
| 👨‍🎓 学生 | `student` | `student123` | 个人训练、AI 教练、查看个人数据 |

> ⚠️ **安全提示**：首次登录后请立即修改默认密码，正式部署时建议删除演示账户。

> 📝 **自助注册**：登录窗口提供「注册」页签，新用户可自助创建**学生**或**教师/家长**账户；管理员账户不开放自助注册，由系统管理员维护。

---

## 5. 角色与权限

| 角色 | 可用功能 |
|------|---------|
| **管理员** | 用户管理（增删改查）、查看所有数据、班级报告、AI 助教 |
| **教师/家长** | 班级报告（所管学生的训练汇总与趋势）、AI 助教咨询、学生分配、导入学生数据 |
| **学生** | 个人训练、AI 教练对话、查看个人记录与成就、导出个人数据 |

主界面按登录角色自动显示对应按钮：

- 所有角色：**开始训练 / 训练设置 / 训练记录 / 🏆 成就 / 🔄 切换账户**
- 教师/管理员额外显示：**📊 班级报告 / 🤖 AI助教**
- 管理员额外显示：**👥 用户管理**
- 学生额外显示：**🤖 AI教练**

---

## 6. 使用指南

### 6.1 训练流程

1. **登录**：输入用户名和密码；首次使用可在登录窗口「注册」页签自助创建账户（学生 / 教师·家长）；
2. **选择模式**：点击「开始训练」，选择**找茬模式**或**动态追踪模式**（也可点击「智能推荐」让系统按历史成绩推荐）；
3. **选择时长**：选择本次训练时长（如 3/5/10 分钟）；
4. **开始训练**：保证面部出现在摄像头画面中、光线充足，跟随游戏提示完成训练；
5. **查看结果**：训练结束后自动生成分析报告（本地 ONNX，可选云端 LLM），可继续与 AI 教练对话或查看记录。

### 6.2 训练模式

| 模式 | 玩法 | 训练目标 |
|------|------|---------|
| 🔍 找茬模式 | 在右侧区域快速扫描并点击隐藏的差异点 | 视觉搜索、持续注意力、反应速度 |
| 🎯 动态追踪模式 | 用鼠标追踪不断移动的红色目标 | 手眼协调、追踪能力、反应时 |

### 6.3 难度说明

| 难度 | 找茬模式 | 动态追踪模式 | 位置随机性 |
|------|---------|-------------|-----------|
| 简单 | 间隔 1500ms，点大小 50px | 间隔 1500ms，点大小 40px | 0.2（集中中心区域） |
| 普通 | 间隔 1500ms，点大小 30px | 间隔 1500ms，点大小 20px | 0.6（中心区域为主） |
| 困难 | 间隔 1500ms，点大小 12px | 间隔 1500ms，点大小 8px | 1.0（全区域随机） |
| 自动 | 根据平均注意力动态推荐 | 同左 | 按推荐难度取值 |
| 自定义 | 用户自由调节所有参数 | 同左 | 1.0 |

**自动难度推荐规则**：最近平均注意力 ≥75 推荐困难，≤50 推荐简单，其余推荐普通；也可手动选择难度。

### 6.4 得分规则

- 每命中一个点得 **10 分**；
- 连击达到 5/10/15/20/25 次后，单次命中得分分别 **+6% / +12% / +18% / +24% / +30%**（25 连击封顶）；
- 未命中每次使单次得分 **降低 5%**（最多累计 -25%），连击 ≥5 次后恢复正常；
- 综合评分（composite score）综合注意力、得分、命中率等维度计算。

### 6.5 成就系统

系统内置 20 项成就（找茬 / 追踪各 10 项），覆盖首次训练、累计次数、连击、高分、完美训练等维度，例如：

- 首次完成训练：「找茬新手」「追踪新手」
- 累计达成：「火眼金睛」（累计找到 100 个差异点）、「坚持之星」（累计训练 60 分钟）
- 高分与完美：「高分突破」（单次 300 分）、「完美游戏」（注意力 80+、得分 500+、连击 10+）

在「🏆 成就」窗口可查看解锁状态与进度条；训练记录窗口提供**趋势图**和**专注度热力图**（行 = 指标，列 = 训练次数）。

### 6.6 AI 功能

| 能力 | 说明 |
|------|------|
| 本地 ONNX 分析 | 训练结束即时生成结构化报告，无需联网 |
| 云端 LLM 分析（可选） | 在设置中填入 OpenAI 兼容 API 后启用，生成深度个性化报告 |
| AI 教练对话（学生端） | 基于训练数据多轮对话，提供针对性指导 |
| AI 助教（教师/管理员） | 班级汇总、单学生下钻、风险提示与训练建议；云端 LLM 不可用时有本地规则兜底 |
| 智能推荐 | 优先使用本地 ONNX 模型（`mode_recommend.onnx`）推荐模式与难度 |

### 6.7 数据导入导出（便携同步）

适用于学生在家训练、到校后把数据交给老师汇总的场景：

1. **学生端**：在训练记录/数据相关入口导出个人训练记录与成就为 JSON 文件；
2. **教师端**：在班级报告或用户管理相关入口导入该 JSON 文件；
3. 系统**按用户名合并**：只新增缺失的记录与更高的成就进度，绝不覆盖其他学生已有的数据（"默认用户"/空用户名数据会被拒绝导入）。

---

## 7. 设置说明

在主界面点击「训练设置」进入设置窗口，左侧导航分为三部分：

### 7.1 训练设置

| 配置项 | 说明 | 推荐值 |
|--------|------|--------|
| EAR 阈值 | 闭眼检测灵敏度，值越小越灵敏 | 0.22 ~ 0.28 |
| 自适应 EAR | 根据历史记录自动调节阈值 | 开启 |
| 眨眼灵敏度 | 1-10 级，越高响应越灵敏 | 5 |
| 界面模式 | 白天 / 夜间 | 根据环境选择 |
| ONNX 使用设置 | 4 个视觉模型（YuNet / OCEC / 6DRepNet / L2CS）按需开关 | 默认关闭，开启后提升精度 |

### 7.2 难度选择

设置默认难度与各难度参数（点大小、出现间隔、位置随机性等），与 6.3 节对应。

### 7.3 壁纸设置

自定义训练界面背景壁纸（加载/绘制/缓存由 `utils/wallpaper_manager.py` 负责）。

### 7.4 AI 助手配置

如需使用云端 LLM 分析，请在设置中填写：

| 配置项 | 说明 |
|--------|------|
| 启用 AI 分析 | 训练结束后自动生成 AI 报告 |
| 本地模型分析 | 使用内置 ONNX 模型，无需联网（默认开启） |
| API 密钥 | OpenAI 兼容接口的 API Key |
| API 地址 | 如 `https://api.openai.com/v1/chat/completions` |
| 模型名称 | 如 `gpt-3.5-turbo`、`deepseek-chat` 等 |

---

## 8. 数据存储与备份

| 运行方式 | 数据位置 |
|---------|---------|
| 源码运行 | `attention_training_py\` 目录下（`attention_data.db`、`users.json`、`time_consumed.db`、`users\` 等） |
| 打包版运行 | **exe 同目录**下（自动创建，绿色便携） |

主要数据文件：

- `attention_data.db`：SQLite 主数据库（用户、训练记录、成就等）
- `users\`：每用户一个子目录（壁纸、成就文件等个性化数据）
- `models\`：ONNX 模型文件（`session_analysis.onnx`、`mode_recommend.onnx` 及 `vision\` 下的 4 个视觉模型）

**备份建议**：直接复制上述数据库文件或整个程序目录即可完成备份；升级版本时保留 exe 同目录数据可避免丢失用户数据。

---

## 9. 项目结构

```
AttentionTraining_v3.0/
├── Intro.md                     # 本说明书
├── APP.zip                      # 打包好的完整应用压缩包（可分发）
├── migrate_to_sqlite.py         # 历史数据迁移脚本
├── App/dist/AttentionTrainingApp/   # 打包产物（exe + 运行库 + 模型）
├── sender/dist/                     # 分发用打包副本
├── docs/                            # 设计文档与实现计划
└── attention_training_py/           # 源码主目录
    ├── main.py                  # 程序入口
    ├── run.py                   # 便捷启动脚本
    ├── build.py / dynamic_build.py / build.bat   # 打包脚本
    ├── requirements.txt         # 依赖清单
    ├── README.md                # 项目说明（开发向）
    ├── ai/                      # AI 模块（本地分析、教练对话、教师助教）
    ├── camera/                  # 摄像头管线（MediaPipe + ONNX 混合）
    ├── core/                    # 核心业务（设置、数据库、用户、成就、LLM 客户端等）
    ├── games/                   # 游戏模块（找茬、动态追踪、得分规则）
    ├── ui/                      # PySide6 界面（主窗口、训练窗口、各对话框）
    ├── utils/                   # 工具（壁纸管理）
    ├── tools/                   # 工具脚本（模型下载、模型训练、验证）
    ├── tests/                   # 单元测试（unittest）
    ├── models/                  # ONNX 模型文件
    ├── 知情同意.md               # 科研测试知情同意书（模板）
    └── 调查问卷.md               # 用户体验反馈问卷（模板）
```

---

## 10. 开发与构建

### 运行测试

```bash
cd attention_training_py
python -m unittest discover -s tests -p "test_*.py"
```

### 打包为 exe

Windows 下一键打包：

```bat
cd attention_training_py
build.bat
```

或使用动态打包脚本（自动收集模块、生成 spec）：

```bash
python dynamic_build.py
```

PyInstaller 配置位于 `pyproject.toml`（应用名 `AttentionTrainingApp`，窗口模式，单目录模式）。

### 模型管理

- 下载 ONNX 视觉模型：`python tools/download_vision_models.py`
- 训练本地分析模型：`python tools/train_local_models.py`
- 验证本地分析与视觉推理：`tools/verify_local_analysis.py`、`tools/verify_onnx_vision.py`

### 扩展开发

- **新增游戏模式**：继承 `games/game_interface.py` 的 `GameInterface`，实现 `start_game()`、`stop_game()`、`update_with_attention()`，再在 `ui/mode_select_dialog.py` 注册入口、`ui/training_window.py` 集成；
- **新增成就**：在 `core/achievement_manager.py` 的 `AchievementType` 与 `_init_achievements()` 中注册，并添加对应 `check_xxx()` 检查逻辑；
- **扩展 AI**：本地分析改 `ai/local_analysis.py`，云端分析改 `core/llm_client.py`，学生端对话改 `ai/coach_logic.py`，教师端对话改 `ai/teacher_coach_logic.py`。

---

## 11. 常见问题

### 摄像头相关

| 问题 | 解决方案 |
|------|---------|
| 摄像头无法打开 | 检查是否被其他程序占用，确认系统权限已授权 |
| 帧率过低 | 降低摄像头分辨率（当前默认 640x480） |
| 人脸检测失败 | 调整光线，正对摄像头，避免逆光；可开启 ONNX YuNet 增强 |

### AI 相关

| 问题 | 解决方案 |
|------|---------|
| ONNX 模型加载失败 | 运行 `tools/download_vision_models.py` 补齐模型文件 |
| 云端 AI 无响应 | 检查 API 密钥、URL 与网络连接；本地分析不受影响 |
| 本地分析较慢 | 确认 CPU 性能，可在 ONNX 使用设置中关闭部分模型 |

### 数据相关

| 问题 | 解决方案 |
|------|---------|
| 训练记录丢失 | 检查数据目录下 `attention_data.db` 是否存在，注意源码/打包版数据位置不同（见第 8 节） |
| 切换用户后数据不显示 | 重新登录或点击刷新 |
| 成就未解锁 | 确认条件已满足，查看成就进度条数值 |
| 导入数据被跳过 | 确认文件是本软件导出的 JSON、版本匹配，且不包含"默认用户"/空用户名数据 |

---

## 12. 注意事项与隐私

1. **摄像头隐私**：系统仅提取面部关键点坐标（数字信息）用于眨眼/视线分析，**不录制、不存储、不上传任何视频画面或照片**；所有生物特征数据本地处理；
2. **密码安全**：正式部署后请立即修改默认密码，并根据实际需要创建真实用户、删除演示账户；
3. **训练安全**：单次训练建议不超过 15 分钟，避免视疲劳；训练时保证光线充足、坐姿端正；
4. **学校科研场景**：仓库内附 `知情同意.md`（家长知情同意书）与 `调查问卷.md`（学生反馈问卷）模板，可按学校信息填写后用于正式测试；
5. **许可说明**：本项目仅供教育学习和研究使用。

---

> 🎯 **让每一次训练都成为进步的阶梯**
